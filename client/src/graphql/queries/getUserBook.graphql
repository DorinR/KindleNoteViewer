query GetUserBook($id: String!) {
    getUserBook(id: $id) {
        error
        book {
            title
            author
            sections {
                sectionHeading
                sectionNotes {
                    note
                }
            }
        }
    }
}
