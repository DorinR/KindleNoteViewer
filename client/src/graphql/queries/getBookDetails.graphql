query GetBookDetails($bookId: String!) {
    getBookHeadings(bookId: $bookId) {
        bookHeadings {
            sectionHeading
            id
        }
    }
    getBookDetails(bookId: $bookId) {
        bookDetails {
            title
            author
        }
    }
}
